node_modules

# segredos — nunca commitar
.env
.env.local
.env*.local

# build do Next.js
.next
out

# outros
*.log
.DS_Store

# planilha de tarefas da Delp — dado confidencial da empresa (nomes de colegas, títulos de
# projeto). A fonte "de verdade" pro Kanban fica no Supabase (upload pela tela /delp-tasks,
# ver src/lib/delpTasks.js), não precisa estar versionada — e não deveria.
contexto/*.xlsx
contexto/*.xls

# código-fonte de OUTRA aplicação do usuário (a ferramenta de controle de testes), colado aqui
# só pra referência/investigação (ver src/lib/sentinelTests.js) — não é parte do Beyond Bits,
# não deveria ser versionado neste repositório.
Controle-e-gest-o-de-testes-main/

# fonte bruta do modelo 3D do Iron Man (.obj de 16MB + .max de 44MB) — só serviu pra rigar no
# Mixamo (ver README em public/models/ironman/). O app só usa os .fbx já rigados, que ficam em
# public/models/ironman/ (esses sim versionados) — a pasta original não precisa estar aqui.
"3d model/"
